# AI 学习手记

一个使用 Astro 构建的静态博客，用于记录 AI 开发的学习、实践与思考。

## 本地运行

```bash
npm install
npm run dev
```

访问 `http://localhost:4321`。

## 构建静态网站

```bash
npm run build
```

构建结果位于 `dist/`。文章写在 `src/content/blog/`，首页与文章页会自动生成对应链接。
